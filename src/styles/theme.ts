import { createTheme } from "@mui/material/styles";

const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#1E40AF",
    },
    secondary: {
      main: "#1E3A8A",
    },
    background: {
      default: "#F3F4F6",
    },
  },
});

export default theme;
